Closure Definition : A closure is a function having access to the parent scope, even after the parent function has closed.



function temporary(){
	let dd = 0;  // parent scope
	return function(){   // closure function
		dd = dd + 1;
	}
}

const add = temporary();
console.dir(add)
add();
add();
