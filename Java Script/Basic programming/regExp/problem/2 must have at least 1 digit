 function Matcching(data){
 	// if(data.length < 3){
 	// 	return false;
 	// }

	let ptr = /[1-9]/g;
	let result = password.match(ptr);
	if(result == null){
		return false;
	}
	return true;
 }

let password = "aaaa1";
console.log(Matcching(password))
