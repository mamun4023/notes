1 . Install nginx on ubuntu 
      sudo apt install nginx

# to find the configuration file in ubunut
      $ cd /etc/nginx

2. check status of nginx server
      systemctl status nginx

3. start server 
      systemctl start nginx  
      service start nginx ( without systemd)
      systemctl enable nginx  ( auto start at boot time/ os starting time)

4. stop server 
      systemctl stop nginx

5. restart server
      systemctl restart nginx  

6. reload nginx 
      systemctl reload nginx  

7. server configuration test
      nginx -t

8. Show all server configuration
      nginx -T

9. See Nginx verserion
      nginx -v
